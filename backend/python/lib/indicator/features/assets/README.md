# Omitted Neural Assets

This public snapshot intentionally excludes trained model artifacts and their run metadata.

The live project contains indicator-owned neural assets used by some research and scoring modules. Those files are not published here because they are generated operational outputs rather than source code.
